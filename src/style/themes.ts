import { createTheme } from '@mui/material/styles';

export const defaultTheme = createTheme({
  palette: {
    primary: {
      main: "#346751",
    },
    secondary: {
      main: "#C84B31",
    },
    background: {
      default: "#ECDBBA",
    },
  },
});
